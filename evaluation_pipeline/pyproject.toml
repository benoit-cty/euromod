[build-system]
requires = ["hatchling>=1.25"]
build-backend = "hatchling.build"

[project]
name = "euromod-evaluation"
version = "0.1.0"
description = "Activity 4 evaluation pipeline: golden dataset + KPI runs over the agentic workflow, results in Postgres."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "anthropic>=0.116",
  "euromod-agentic-workflow",
  "psycopg[binary]>=3.2",
  "pydantic>=2.8",
  "python-dotenv>=1.0",
  "typer>=0.12",
]

[project.optional-dependencies]
test = ["pytest>=8"]

[project.scripts]
euromod-eval = "euromod_eval.cli:app"

[tool.uv.sources]
euromod-agentic-workflow = { path = "../agentic-workflow/pipeline", editable = true }

[tool.hatch.build.targets.wheel]
packages = ["src/euromod_eval"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
